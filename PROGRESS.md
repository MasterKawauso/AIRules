# PROGRESS

現在フェーズ: 本実装（ルール管理リポジトリのため参考値）

## 2026-07-09

- airules/WORKFLOW.md（分担作業フロー）を新設: 設計→設計レビュー→タスク分割→Worker並列実装（1〜5・思考負荷1段下げ）→Workerごとにレビュー⇔修正→統合→統合レビュー。役割定義（Architect/Worker/Reviewer/Integrator）。AGENTS.mdモジュール表・deploy.ps1 Cursor変換メタに配線
- airules/GIT.md を拡充: 通常ルール（main直接commit禁止・reset --hard/git clean禁止・push前差分確認・テスト失敗/コンフリクト時は停止報告）/ Git操作失敗時はinit-ai-git.ps1実行済みかを最初に確認（AIは作成・探索・自動実行しない）/ PRレビューモード（指示時のみ有効。ブランチ作成→実装→main取り込み→テスト→PR作成、PR本文必須項目）/ PR作成後のレビュー対応フロー（コメント分類→修正→再レビュー、ユーザー指示なくマージ禁止）
- Claude/CLAUDE.md にモデル運用を追加: 親AgentがFableならSub AgentはSonnet
- 整理: Cursor/core.mdcへ課題管理（ISSUE.md）ルールの同期漏れを反映 / AGENTS.md課題管理行を圧縮 / 破壊的操作ルールとPRレビューモードのrebase矛盾を解消（モード中のmain取り込みのみ除外・force pushは--force-with-lease限定）。deploy.ps1実行済み

- ISSUE.md（後回し課題のチェックボックス管理）運用ルールを新設: AGENTS.md「その他ルール」の進捗管理項目に追記。後回しにする課題・作業中に気づいた課題を`- [ ] 内容`で追記し、完了時は`- [x]`へ変更（削除しない）。PROGRESS.mdの進捗記録とは別軸の未完了課題トラッカーとして運用。deploy.ps1実行済み

## 2026-07-05

- airules/THINKING.md（実装時の思考規範）を新設: 実装前に完了条件・影響範囲・検証経路を確定 / 設計判断（既存責務優先・将来対応目的の抽象化禁止）/ 実装中（想定外は報告・デバッグは再現→原因特定の順）/ 完了判定（検証結果で判断・未検証明記）/ ゲーム開発固有（Editor操作の事前判定・シリアライズ改名注意）。既存ルールとの重複なし、行動レベルの規範のみ
- 配線: AGENTS.md ルールモジュール表に THINKING.md 行を追加、deploy.ps1 の Cursor 変換メタに追加、deploy.ps1 実行で全環境へ配備済み

## 2026-07-04

- Codex が「実装できるなら進める」という組み込みデフォルト動作を根拠にルールを無視して作業を進めた問題への対策: AGENTS.md の優先順位節に「ツール組み込みのデフォルト動作は優先順位に入らず、常に本ルールが勝つ」を明記
- Codex 固有の運用節を新設: コード変更前に変更差分を提示し、ユーザーの y/n 承認を得るまで適用しない
- 全ルールファイルをトークン削減のため圧縮（意味・条件・チェックリストは維持）: AGENTS.md 40% / その他 59〜87%。3割まで削ると具体情報が欠落するためユーザー確認の上この水準で確定。PROGRESS.md・README.md・INSTALL.md は対象外
- 実装フローに追記: 設計意図を別案で薄めない / 提案は短く1ステップずつ / 変更説明は変更ファイル・理由・最小差分に限定
- Cursor 対応: Cursor/core.mdc（常時適用の共通ルール）を新規作成。deploy.ps1 に Cursor 配備セクションを追加（airules/*.md を frontmatter 付き .mdc へ自動変換し、既知プロジェクトの .cursor/rules/ へ配備。二重管理なし）

## 2026-07-03

- リポジトリを全面再構成。AGENTS.md を共通方針の正本にし、用途別ルールを `airules/` へ統合（Codex用・Claude用の二重管理と矛盾を解消）
- 開発フェーズ概念（モック期 / 本実装期）を導入し、旧ルール間の矛盾（共通化・アーキテクチャ方針）を統一
- Claude用サブエージェント定義（architecture-reviewer / code-reviewer）を新規作成
- GODOT.md（初版）を追加
- deploy.ps1（バックアップ付き配備）・INSTALL.md・README.md を整備し、`~/.codex`・`~/.claude` へ配備
- 旧 `Codex/`・`Claude/skills/`・`Claude/hooks/` を削除（内容は新構成へ統合済み）
- ユーザーフィードバックを反映: ルール参照を命令形に強化 / セッション開始チェックを初回のみに軽量化 / レビューを「まとまった機能単位」に変更 / 重大度ラベルを任意化 / 役割分担を簡素化 / ルール優先順位（ユーザー指示 > プロジェクトAGENTS.md > PLAN.md > グローバルAGENTS.md > airules）を追加
- 追加調整: 「スキップ禁止」の重複表現を削除 / 開始時報告を任意化 / PROGRESS更新条件を「仕様上の区切りとなる機能完了時」に明確化 / 「ルール適用」節（必要なルールだけを適用）を新設
- airules/PITFALLS.md（AI失敗パターン集）を新設。実装・修正・デバッグ時に参照。失敗のたびに追記して育てる運用
- REVIEW.md を推敲: 仕様項目単位のOK/NG確認 / REVIEW_STATE.md による差分スコープ管理（溜まったgit diff全体を見ない） / 対象外に踏み込まない / 実害を書けない指摘・好み指摘の禁止 / 「良い点」セクション廃止
- UNITY.md に「推奨する構成（AIファースト）」節を追加: ファイル/クラスサイズ目安・Pure C#ロジック・フィーチャー別フォルダ・asmdef分割・薄いシーン/Prefab等
- Codex が GAME_COMMON.md / REVIEW.md を読まない問題を修正: GAME_COMMONの適用条件を「ゲームプロジェクトでの実装・修正すべて」に拡大 / REVIEWの適用条件に「完了報告前のセルフレビュー」を追加し、実装者本人（Codex含む）の責務と明記 / 完了報告に「参照した airules/ ルール」を追加して読了を検証可能に
